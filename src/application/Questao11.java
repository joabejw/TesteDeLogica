package application;

public class Questao11 {
	/*
	 * Interfaces: Em orientação a objetos, uma interface é um conjunto de métodos
	 * abstratos que definem um contrato a ser implementado pelas classes que a
	 * implementam. As interfaces são usadas para estabelecer um padrão de
	 * comportamento entre diferentes classes, sem impor uma implementação
	 * específica. Em Java, por exemplo, as interfaces são declaradas com a
	 * palavra-chave interface.
	 * 
	 * Instâncias: Quando uma classe é instanciada, é criada uma nova instância (ou
	 * objeto) daquela classe. Uma instância é uma cópia da classe, com seus
	 * próprios valores de atributos e métodos. As instâncias são criadas a partir
	 * de uma classe e representam objetos reais em tempo de execução. Em Java, por
	 * exemplo, as instâncias são criadas com a palavra-chave new.
	 * 
	 * Objetos: Na orientação a objetos, um objeto é uma instância de uma classe que
	 * tem um estado (atributos) e comportamento (métodos). Um objeto pode ser
	 * criado a partir de uma classe e pode interagir com outros objetos para
	 * realizar operações complexas. Por exemplo, em um sistema bancário, podemos
	 * ter objetos do tipo Conta, Cliente e Transação, que interagem entre si para
	 * realizar operações bancárias.
	 * 
	 * Classes: Uma classe é um modelo ou molde para criar objetos. Ela define a
	 * estrutura e o comportamento que os objetos de sua classe terão. As classes
	 * contêm atributos (dados) e métodos (comportamento) que definem o estado e o
	 * comportamento dos objetos criados a partir delas. Em Java, por exemplo, as
	 * classes são declaradas com a palavra-chave class.
	 * 
	 * Em resumo, as interfaces definem um contrato a ser implementado por classes,
	 * as instâncias são cópias de classes criadas em tempo de execução, os objetos
	 * são instâncias de classes com um estado e comportamento próprios e as classes
	 * são modelos para criar objetos com atributos e métodos. Cada um desses
	 * conceitos é fundamental na orientação a objetos e deve ser entendido para
	 * desenvolver software orientado a objetos eficiente e escalável.
	 */
}